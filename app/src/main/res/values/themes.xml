<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <!--   TODO STEP 2: set primary brand colors to theme by overriding colorPrimary, colorPrimaryVariant and colorOnPrimary from colors.xml. note to use attributes that supports AppCompat and not android: platform attributes.-->
        <!-- Secondary brand color. -->
        <!--   TODO STEP 3: set secondary brand colors to theme by overriding colorSecondary, colorSecondaryVariant and colorOnSecondary from colors.xml. note to use attributes that supports AppCompat and not android: platform attributes.-->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Toolbar -->
    <style name="Theme.ThemesAndStylesWorkshop.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ThemesAndStylesWorkshop.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>